# Example environment overrides for local runs.
# Copy to config/.env or export directly in your shell.

# Repository root override (optional).
FLYBEHAVIOR_RESPONSE_REPO_ROOT=/path/to/FlyBehaviorScoring

# Outputs directory override (optional).
FLYBEHAVIOR_RESPONSE_OUTPUTS_DIR=/path/to/FlyBehaviorScoring/outputs

# Default config directory override (optional).
FLYBEHAVIOR_RESPONSE_CONFIG_DIR=/path/to/FlyBehaviorScoring/config

# Data directory override (optional).
FLYBEHAVIOR_RESPONSE_DATA_DIR=/path/to/FlyBehaviorScoring/data

# FlyPCA default config path override (optional).
FLYPCA_CONFIG_PATH=/path/to/FlyBehaviorScoring/config/example.yaml
